# Preserved duplicates

Files in this directory were confirmed as byte-for-byte duplicates of a canonical file and moved out of active locations rather than deleted.

- `test_alignment_metrics.root-copy.tsv` duplicates `alignment_analysis/scripts/30x/tests/fixtures/test_alignment_metrics.tsv`.
