# Alignment-analysis tests

- `manual_runs/`: retained outputs from small manual mapper and VG validation runs.
- [`../scripts/30x/tests/fixtures/`](../scripts/30x/tests/fixtures/): small deterministic fixture used by the 30× analysis code.

Manual-run BAM files are generated artifacts and are not inputs to the canonical 30× table.
