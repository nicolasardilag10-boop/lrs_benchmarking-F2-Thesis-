# Tool inventories

- [`caller_tools_inventory.csv`](caller_tools_inventory.csv): inventory of candidate and selected variant-calling tools.

Validation-specific inventories may also be present under `docs/variant_caller_validation/` and `SV aligners call/docs/`.
