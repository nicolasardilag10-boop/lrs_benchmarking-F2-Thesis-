# Historical code snapshots

Timestamped and `before_*` copies moved out of the active workflow directory. They are retained only to reconstruct development history.

- `config/`: old configurations and sample-sheet backups.
- `workflow/`: old Snakefile revisions.
- `scripts/`: old alignment-metric plotting revisions.

Use `../../config.yaml`, `../../samples.tsv`, `../../snakemake_aligners.smk`, and `../../scripts/` for current work.
