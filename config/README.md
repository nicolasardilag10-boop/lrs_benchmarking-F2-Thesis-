# Shared configuration

Reserved for project-wide configuration. Configuration used by a single workflow should remain beside that workflow, such as `assemblers/benchmark_chr21_real/config/` or `final_report_files/snakemake_aligners_benchmarking/config.yaml`.
